import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cuozzogennaro.ratiomaster',
  appName: 'Ratio Master',
  webDir: 'dist/client'
};

export default config;
